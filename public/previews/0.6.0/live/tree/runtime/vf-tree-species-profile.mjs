const PROFILES = Object.freeze([
  [0.98, 0.30, 0.72, 0.66, 0.14, 0.56, 1.20, 1.05, 0.56, 0.52, 7, 0.055],
  [1.04, 0.32, 0.69, 0.64, 0.11, 0.60, 1.08, 1.18, 0.58, 0.54, 9, 0.045],
  [0.92, 0.28, 0.74, 0.62, 0.17, 0.52, 1.28, 1.12, 0.52, 0.49, 6, 0.065],
  [1.08, 0.34, 0.67, 0.68, 0.09, 0.62, 1.16, 1.26, 0.60, 0.57, 10, 0.04],
  [0.96, 0.31, 0.71, 0.65, 0.13, 0.58, 1.24, 1.20, 0.55, 0.51, 8, 0.052],
].map((values, speciesIndex) => Object.freeze({
  kind: 'tree-species-profile:v1',
  speciesIndex,
  pathLength: Object.freeze({
    scaleMean: values[0],
    scaleDeviation: 0.075,
    scaleBounds: Object.freeze([0.78, 1.2]),
    rootConsumptionMean: values[1],
    rootConsumptionDeviation: 0.025,
  }),
  split: Object.freeze({
    allometricExponent: 2,
    areaLossMean: Math.min(0.88, values[2] + 0.14),
    areaLossDeviation: 0.035,
    areaLossBounds: Object.freeze([0.78, 0.9]),
    mainAreaShareMean: values[3],
    mainAreaShareDeviation: 0.035,
    mainAreaShareBounds: Object.freeze([0.56, 0.73]),
    mainAngleMean: values[4],
    mainAngleDeviation: 0.035,
    mainAngleBounds: Object.freeze([0.045, 0.25]),
    lateralAngleMean: values[5],
    lateralAngleDeviation: 0.1,
    lateralAngleBounds: Object.freeze([0.3, 0.88]),
    mainBudgetRatio: Object.freeze([0.88, 0.98]),
    lateralBudgetRatio: Object.freeze([0.66, 0.88]),
  }),
  curvature: Object.freeze({
    meanTurn: 0,
    trunkDeviation: 0.004,
    radiusDeviationRise: 0.052 + speciesIndex * 0.0015,
    radiusExponent: 1.25,
    correlation: 0.82,
    maximumTurn: 0.14,
    steps: Object.freeze({ trunk: 7, branch: 5, twig: 3 }),
  }),
  crownEnvelope: Object.freeze({
    axisScaleMean: Object.freeze([values[6], values[7], values[8]]),
    axisScaleDeviation: Object.freeze([0.08, 0.08, 0.045]),
    centerHeightMean: values[9],
    centerHeightDeviation: 0.035,
    centerHorizontalDeviation: 0.08,
    orientationDeviation: 0.42,
    localAttractionBounds: Object.freeze([0.28, 0.72]),
  }),
  colonization: Object.freeze({
    attractionPointCount: 144,
    candidateCount: 8,
    alignmentFloor: 0.12,
    directionBlend: 0.42 + speciesIndex * 0.015,
    lightWeight: 0.46,
    spaceWeight: 0.34,
    alignmentWeight: 0.2,
  }),
  twig: Object.freeze({
    shootSlots: 2,
    shootProbabilityFloor: 0.3,
    shootProbabilityRise: 0.42,
    shootProbabilityExponent: 0.55,
    shootRadiusRatioBounds: Object.freeze([0.07, 0.12]),
    shootPathBudgetRatioBounds: Object.freeze([0.28, 0.5]),
    shootLengthRatioBounds: Object.freeze([0.84, 0.98]),
    shootAngleMean: 0.82,
    shootAngleDeviation: 0.16,
    shootAngleBounds: Object.freeze([0.48, 1.18]),
    phyllotaxisDivergence: Math.PI * (3 - Math.sqrt(5)),
    phyllotaxisJitterDeviation: 0.11,
    terminalLeafCountMean: 1,
    terminalLeafCountDeviation: 0.3,
    terminalLeafCountBounds: Object.freeze([1, 2]),
    shootLeafCountMean: 10,
    shootLeafCountDeviation: 0.6,
    shootLeafCountBounds: Object.freeze([9, 11]),
    attachmentBounds: Object.freeze([0.05, 0.82]),
    attachmentBiasExponent: 1.35 + speciesIndex * 0.025,
    leafScaleBounds: Object.freeze([0.024, 0.034]),
  }),
  bark: Object.freeze({
    featureGrammar: Object.freeze(['ridge', 'furrow', 'fissure', 'lenticel']),
    ridgeCountMean: Math.round(values[10] * 1.5),
    ridgeCountDeviation: 1.2,
    ridgeCountBounds: Object.freeze([10, 18]),
    ridgeAmplitudeMean: values[11],
    ridgeAmplitudeDeviation: 0.012,
    ridgeAmplitudeBounds: Object.freeze([0.025, 0.085]),
    roughnessVariation: 0.22,
    colorVariation: 0.22,
    normalStrength: 0.66 + speciesIndex * 0.02,
    fissureSharpness: 7 + speciesIndex * 0.45,
    fissureAxialFrequency: 72 + speciesIndex * 4,
    grainAxialFrequency: 184 + speciesIndex * 9,
    microRidgeWeight: 0.24 + (speciesIndex % 3) * 0.025,
    microRidgeAxialFrequency: 46 + speciesIndex * 3,
    textureVariantWeights: Object.freeze([4 + speciesIndex, 3, 2 + (speciesIndex % 2)]),
  }),
})));

export function treeSpeciesProfileReference(speciesIndex) {
  if (!Number.isSafeInteger(speciesIndex) || speciesIndex < 0 || speciesIndex >= PROFILES.length) {
    throw new RangeError(`tree species profile index must be in [0, ${PROFILES.length - 1}]`);
  }
  return PROFILES[speciesIndex];
}
